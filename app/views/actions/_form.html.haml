-# Form builder is passed in as 'form'
= form.input :_type, :collection => @available_action_types, :include_blank => false
- form.object.keys.reject {|k, v| ["_id", "_type", "action_text"].include? k }.each do |k, v|
  = form.input k
= form.input :action_text
