-# Form builder passed in as form
.window.code_box
  = form.input :id, :as => :hidden
  = form.input :_destroy, :as => :hidden, :input_html => {:value => 0, :class => "destroy"}
  = form.input :shortcode
  %a.delete_code delete this code
  = form.input :splash_id, :as => :hidden
